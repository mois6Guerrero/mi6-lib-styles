import primary  from './semantic/primary';
import focusRing  from './semantic/focus.ring';
import colorScheme from './semantic/color.scheme';

export default {
  primary,
     // Borde de enfoque global (inputs, botones, etc.)
  transitionDuration: '0.2s',
  
  focusRing,
   // Light / Dark (usa tu selector data-theme="dark")
  colorScheme
};
        

